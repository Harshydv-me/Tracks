services:
  # Backend Web Service
  - type: web
    name: skills-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm run db:setup && npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: skills-db
          property: connectionString
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_HOST
        fromService:
          type: web
          name: skills-frontend
          property: host
      - key: CLIENT_URL
        value: https://${FRONTEND_HOST}.onrender.com
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-1.5-flash
      - key: NODE_VERSION
        value: 20

  # Frontend Static Site
  - type: web
    name: skills-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: BACKEND_HOST
        fromService:
          type: web
          name: skills-backend
          property: host
      - key: VITE_API_BASE
        value: https://${BACKEND_HOST}.onrender.com/api
      - key: NODE_VERSION
        value: 20

databases:
  - name: skills-db
    plan: free
